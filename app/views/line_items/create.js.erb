$('.flash').hide();

if ($('#cart tr').length > 0) { $('#cart').show('blind', 1000); }

$('#cart').html("<%= escape_javascript render(@cart) %>");

$('.current_item').css({'background-color':'#FF8C00'}).
animate({'background-color':'transparent'}, 1000);